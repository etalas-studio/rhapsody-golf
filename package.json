{
  "name": "rhapsody-golf-connect",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,yellow \"bun run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && bun run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && bun run build",
    "install:all": "cd frontend && bun install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "prisma": "^6.19.3"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  }
}
